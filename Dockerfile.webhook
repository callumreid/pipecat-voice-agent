FROM python:3.12-slim

WORKDIR /app

COPY requirements.webhook.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY sip_webhook.py .

EXPOSE 8080
CMD ["python", "sip_webhook.py"]
